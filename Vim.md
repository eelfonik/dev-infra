`esc` to normal mode
- `$` to move the cursor to the end of the line
- `G` ( `shift` + `g`) to move the cursor to the last non-whitespace character.
- `d` + `d` to delete the whole line from the start of the line

`V` ( `shift` + `v`) to enter visual mode